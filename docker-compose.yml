version: '3'
services:
  db:
    image: postgres:latest
    env_file:
      - .env
    ports:
      - '5432:5432'
  redis:
    container_name: cache
    image: redis
    ports:
      - 6379:6379
    volumes:
      - redis:/data

volumes:
  redis:
    driver: local
